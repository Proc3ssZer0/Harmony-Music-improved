name: harmonymusic
description: A cross platform app for music streaming.
publish_to: "none"

version: 1.12.2+27

environment:
  sdk: ">=3.4.0 <4.0.0" # Обновили минимальный порог под свежий Flutter

dependencies:
  animations: ^2.0.11
  app_links: ^7.0.0
  archive: ^4.0.9
  audio_service: ^0.18.17
  audio_service_mpris: ^0.2.1
  audio_video_progress_bar: ^2.0.3
  audiotags: ^1.4.1
  buttons_tabbar: ^1.3.13
  cached_network_image: ^3.4.0
  dio: ^5.7.0
  file_picker: ^12.0.0-beta.1
  flutter:
    sdk: flutter
  flutter_keyboard_visibility: ^6.0.0
  flutter_lyric: 2.0.4+6
  flutter_slidable: ^4.0.3
  get: ^4.7.1
  google_fonts: ^8.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  html: ^0.15.6
  ionicons: ^0.2.2
  jni: ^0.9.2
  just_audio: ^0.10.5
  just_audio_media_kit: ^2.1.0
  palette_generator: ^0.3.3+4
  path: ^1.8.3
  path_provider: ^2.1.1
  permission_handler: ^12.0.1
  share_plus: ^13.1.0
  package_info_plus: ^10.1.0
  wakelock_plus: ^1.3.3
  shimmer: ^3.0.0
  sidebar_with_animation:
    git:
      url: https://github.com/anandnet/animated_side_bar.git
      ref: main # Подтягиваем последние правки
  smtc_windows: ^0.1.2
  terminate_restart:
    git:
      url: https://github.com/anandnet/terminate_restart.git
      ref: main
  toggle_switch: ^2.1.0
  tray_manager: ^0.5.2
  url_launcher: ^6.2.1
  widget_marquee: ^0.0.8
  window_manager: ^0.5.1
  youtube_explode_dart: ^2.4.0 # Стабильная версия с поддержкой новых API YouTube
  
  # Добавляем для будущего дизайна
  flutter_animate: ^4.5.0 # Для очень плавных анимаций в одну строку
  glassmorphism: ^3.0.0 # Для того самого эффекта блюра

dev_dependencies:
  flutter_lints: ^6.0.0 # Обновили линты под стандарты 2026 года
  flutter_test:
    sdk: flutter
  jnigen: ^0.16.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
